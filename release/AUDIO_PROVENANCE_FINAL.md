# Audio provenance — final record (Part II, §9-§12)

**Status: generated audio landed (run 33648420215, commit `61c88ee`); the reception re-recording of six partner lines is the last open item (see Results).** The census and decisions below are final.

## Census before this program

| Asset group | Files | Source | Class |
|---|---|---|---|
| Course recordings (de, es, fr, it, ja, ko, pt, zh) | 856 MP3 | upstream commit `bc44daf` ("Initial Lingo Lessons mobile app"); README: "pre-generated TTS MP3s (generated — don't edit by hand)"; generator `scripts/gen-audio.ts` never published; encoder tags show only ffmpeg/LAME (`Lavf58.76.100`, `Lavf61.7.100`, `LAME3.100`), no vendor tag | **D — UNKNOWN / UNPROVEN** |
| Sound effects (`correct.wav`, `incorrect.wav`, `finish.mp3`) | 3 | same initial commit; no source or license statement | **D — UNKNOWN / UNPROVEN** |
| French reception clips (`assets/audio/fr-reception`) | 167 MP3 | Phase-7 pipeline: Piper voices `fr_FR-siwis-medium` (CC BY 4.0) and `fr_FR-upmc-medium` (CC BY-SA 4.0), pinned by hash, MODEL_CARDs committed | **B — PROJECT-GENERATED WITH DOCUMENTED PIPELINE** |

### Tracing attempt (§10)

Searched: the complete upstream history (11 commits), README, the only
LICENSE file (MIT, code), all tracked text files for TTS/voice/license
mentions, the audio manifest header ("Generated by scripts/gen-audio.ts"),
and the binary tags of every language directory. Result: the recordings
were produced by an unpublished script from an unnamed text-to-speech
service; **no license or terms for the audio exist anywhere**. The
repository's MIT grant covers the code; it does not establish that the
generator's provider permitted redistribution of its output. That is an
unproven redistribution right, so under §9 the recordings do not ship.

## Decisions (§11)

| Course | Decision | Why |
|---|---|---|
| fr-en | regenerate with `fr_FR-siwis-medium` (the reception voice) | one speaker across words, sentences and listening clips |
| de-en, es-en, pt-en | regenerate with a license-gated Piper voice selected by the canary ASR audit | recon run 33646858305 (2026-09-02) admitted de_DE-thorsten-medium (CC0), de_DE-mls-medium (CC BY 4.0), es_ES-davefx-medium (CC0), pt_BR-faber/cadu/jeff-medium (CC0); es_ES-sharvard-medium was excluded (CC BY 3.0 is outside the project's allow list) |
| it-en, zh-en | **device text-to-speech** (`audio.policy = device-tts`) | the only Italian medium voice (paola) states "License: See URL" and the only Mandarin voice (huayan) states "License: Unknown" — both excluded by the fail-closed gate in the same run; an unverifiable license is not a redistribution right, and the extra-low-quality Italian alternative is not release quality |
| ja-en, ko-en | **device text-to-speech** (`audio.policy = device-tts` in the pack, reason recorded) | rhasspy/piper-voices v1.0.0 ships no Japanese or Korean voice; the runtime already spoke unknown strings through `expo-speech`, so course behaviour is preserved with the OS voice |
| sound effects | regenerate from pure ffmpeg tone expressions | provenance-clean by construction; same file names so no code change |
| Phase-10 residuals (four Section-1 prompts without audio; six partner lines) | closed: the four `audioTarget`s are restored by the pipeline's `restoreAudioTargets`; the six partner lines are re-synthesized by the reception workflow after the text edits | no P2 audio item is deferred |

## Pipeline guarantees

- Voices pinned by double-download SHA-256 (`content/audio/pack-audio-manifest.json`); MODEL_CARDs preserved verbatim under `content/audio/model-cards/`.
- License gate: allow CC BY 4.0 / CC BY-SA 4.0 / CC0; deny AGPL, GPL, NC, ND; a card that matches nothing excludes the voice.
- Every clip: technical QA (duration, level, edge silence, speaking-rate band per language) and an ASR audit in the clip's language; strict QA gates the generate run.
- Assets are content-addressed (`sha256(course, voice, text, rate, pipelineVersion)`), the runtime manifest is regenerated as a whole, and `content/audio/legacy-baseline.json` is re-blessed to the generated set so CI verifies every shipped file.
- No paid service, no runtime model, no network at runtime.

## Results

Generate run **33648420215** (workflow `pack-audio.yml`, mode `generate`, courses de-en, es-en, fr-en, pt-en; commit `61c88ee`). ASR model for the shipped-clip audit: `medium` (faster-whisper 1.0.3, 1.0 s / 0.5 s silence padding, no cross-clip conditioning). Strict technical QA gated the run.

| Course | Voice | License | Clips generated | Files on disk | Technical failures | Mean WER (all clips, informational) |
|---|---|---|---|---|---|---|
| de-en | `de_DE-thorsten-medium` | CC0-1.0 | 95 | 95 | 0 | 0.1728 |
| es-en | `es_ES-davefx-medium` | CC0-1.0 | 189 | 189 | 0 | 0.2521 |
| fr-en | `fr_FR-siwis-medium` | CC-BY-4.0 | 164 | 164 | 0 | 0.1687 |
| pt-en | `pt_BR-faber-medium` | CC0-1.0 | 96 | 96 | 0 | 0.3453 |

- 544 clips, 544 unique content-addressed assets, 0 technical failures (duration, level, edge silence, speaking rate all within band).
- The mean WER includes single-word clips, where a Whisper-family recognizer is unreliable by construction; the actionable signal is the sentence-level review list below.
- The four Section-1 prompts that had no audio since Phase 10 (`fr-en:u0-l1-e6/e7/e8/e10`, *C'est un garçon.* / *C'est une fille.*) are restored and voiced.
- Italian, Japanese, Korean and Chinese ship no bundled audio (`audio.policy = device-tts`); their upstream recordings were removed.

### Sentence clips flagged for human review (WER ≥ 0.5 on a ≥ 3-word transcript)

| Clip | WER | Recognizer heard | Duration |
|---|---|---|---|
| `es-en:El perro bebe agua.` | 0.5 | 'Y luego bebe agua.' | 0.784 s |
| `es-en:La vaca come.` | 1 | "¡Da pa'cacones!" | 1.019 s |
| `es-en:No quiero beber té.` | 0.5 | '¡No quiero beberte!' | 1.176 s |
| `fr-en:L'oiseau vole haut.` | 0.5 | "L'oiseau-velo" | 1.019 s |
| `pt-en:A ovelha é mansa.` | 0.75 | 'Alvelha mansa' | 1.123 s |

Disposition: five of 544 clips. Each is a short sentence where the recognizer's error is plausible on a 1–2 s clip (dropped article, homophone, run-together words); none can be judged by ear in this environment. They are listed in `DEVICE_ACCEPTANCE.md` row I4/A5 for the owner to play on the RC (Learn → the lesson that carries the sentence). If any is unintelligible on a device it is a P2: re-synthesize that string (the pipeline is incremental by asset key) — never ship it silently.

### Sound effects (generated tone sequences, provenance-clean by construction)

| File | Bytes | SHA-256 |
|---|---|---|
| `correct.wav` | 28,302 | `d3cdb13d42778d22dc83e95e898fafb97a0bfd444c9ee9a4646d4bf0ba3899fd` |
| `finish.mp3` | 11,956 | `7653b8077c9b6e2b4303064b18e844307c8cc8d9f4fcd3c62619b63b3d166ce8` |
| `incorrect.wav` | 32,712 | `c5ee84976de623761993b4426bc2ca7d7e5898aac44751cd24b4da8ee8f55be3` |

### Reception audio (six re-worded partner lines)

The six clips whose text changed (`KNOWN_LIMITATIONS.md` L10) are re-synthesized by the reception-audio workflow in incremental mode: the 161 untouched clips are reused byte-for-byte, the six superseded files are pruned, the runtime clip map and the audio baseline are regenerated by the run. Voice `fr_FR-upmc-medium` (CC BY-SA 4.0), pinned. Run id and quality summary are appended below when it lands.

Census after apply (`content/reports/pack-audio-census.json`): {"generator": "scripts/pack-audio.ts apply", "pipelineVersion": 1, "sfx": ["correct.wav", "incorrect.wav", "finish.mp3"]}

**Status: CLOSED for the four generated courses and the sound effects; reception line re-recording in flight (see below).**

